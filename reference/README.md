# Reference Template

Place a MOV from a Live Photo that has been verified to animate on the target iPhone Lock Screen in this directory.

The generator uses this MOV only as a metadata-track template. Do not combine its still image or video content with files from `input/`.
